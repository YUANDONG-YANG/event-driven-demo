/**
 * Local storage root, startup preparation, directory watch, file detection events, and payload construction for
 * Event Grid / Service Bus (aligned with ingest payload fields).
 */
package com.demo.azure.storage;
